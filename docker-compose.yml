# Gateway + worker (Phase 4). cpp-frame joins the same network in a later phase.

services:
  gateway:
    build: ./gateway
    ports:
      - "8000:8000"

  worker:
    build: ./worker
    ports:
      - "8001:8001"
