# Single-service stack for now (Phase 3). The gateway will later be joined
# by worker and cpp-frame on the same default Docker network.

services:
  gateway:
    build: ./gateway
    ports:
      - "8000:8000"
