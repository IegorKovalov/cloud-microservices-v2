# Gateway, worker, cpp-frame share the default Compose network (Phase 7+).

services:
  gateway:
    build: ./gateway
    environment:
      WORKER_BASE_URL: http://worker:8001
    ports:
      - "8000:8000"

  worker:
    build: ./worker
    environment:
      CPP_FRAME_BASE_URL: http://cpp-frame:8002
    ports:
      - "8001:8001"

  cpp-frame:
    build: ./cpp-frame
    ports:
      - "8002:8002"
