// Phase 6: minimal C++ program. Later this folder becomes the cpp-frame HTTP
// service (main.cpp). For now we only prove the toolchain and a clean build.

#include <iostream>

int main() {
  std::cout << "hello\n";
  return 0;
}
